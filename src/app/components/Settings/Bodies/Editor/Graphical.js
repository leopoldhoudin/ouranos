import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  color: white;
`;

const Graphical = ({body}) => <Container>Graphical</Container>;

export default Graphical;
