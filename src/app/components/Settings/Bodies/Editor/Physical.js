import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  color: white;
`;

const Physical = ({body}) => <Container>Physical</Container>;

export default Physical;
