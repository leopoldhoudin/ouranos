import React from 'react';
import styled from 'styled-components';

const Layout = styled.div`
  display: flex;
  flex-direction: column;

  width: 100%;

  color: yellow;
`;

const Keplerian = ({params, onChange}) => {
  return (
    <Layout>
      KEPLERIAN
    </Layout>
  );
};

export default Keplerian;
